{
    "version": "0.2.0",
    "configurations": [
      {
        "name": "Launch Package",
        "type": "go",
        "request": "launch",
        "mode": "auto",
        "program": "${workspaceFolder}",  // Set to the root folder or where your Go source files are located
        "args": [
          "INFOR-DOC2.ionapi",
          "--debug",
          "--check_m3"  // The argument to pass (ensure correct file path)
        ]
      }
    ]
  }